# Implementation Plans

Generated by the improve skill on 2026-06-20. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix worker onerror orphaning pending tasks | P1 | S | — | DONE |
| 002 | Scope env.reset() to only clear rsa:env: keys | P1 | S | — | DONE |
| 003 | Add typecheck to CI pipeline | P1 | S | — | DONE |
| 004 | Use configurable stallTimeout in error messages | P2 | S | — | DONE |
| 005 | Reduce history in-memory cap and truncate sensitive fields | P2 | S | — | DONE |
| 006 | Sanitize user input before Python interpolation in SageMath templates | P2 | M | — | DONE |
| 007 | Escape HTML entities in ProofRenderer inline text | P3 | S | — | DONE |
| 008 | Track non-success frontendCheck results in Magic execution | P2 | S | — | DONE |
| 009 | Make extractFrontendCheck brace counter string/regex-aware | P3 | M | — | DONE |
| 010 | Establish test coverage baseline for critical paths | P2 | L | — | DONE |
| 011 | Add architectural decision records for key tradeoffs | P3 | S | — | DONE |
| 012 | Add prefill support to PEM Decryptor action buttons | P3 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- All 12 plans are independent — no cross-plan dependencies.
- Plan 010 (test baseline) should be executed early to provide regression safety for other changes, but is not a hard dependency.

## Findings considered and rejected

- **`new Function()` in PoW worker** (`pow-worker.ts:67-68`): By-design — power users need custom check logic. Documented in UI.
- **`useAttackExecution` finally unconditionally aborts**: Aborting a completed controller is a no-op; harmless.
- **Sidebar FactorDB health check on mount**: Standard pattern for service status indicators; low overhead.
- **`useSageMathParallel` recreates executor per batch**: Minor DOM element accumulation, cleaned up on timeout. Not worth the complexity of memoization.
- **`combineSignals` fallback potential listener leak**: Executions are bounded by timeouts; listeners always fire and clean up.
