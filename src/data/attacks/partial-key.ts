import type { Attack } from '../../types';

export const partialKeyAttacks: Attack[] = [
  {
    id: 'simple-lattice',
    name: 'Simple Lattice',
    category: 'Partial Key / Lattice',
    description: 'Recovers full factorization from an approximate value of p using Coppersmith.',
    inputs: [
      { name: 'n', label: 'n (modulus)', placeholder: 'Enter modulus n...', multiline: true, rows: 3 },
      { name: 'nearp', label: 'nearp (approximate p)', placeholder: 'Enter approximate p value...', multiline: true, rows: 3 },
    ],
    sageTemplate: (v) => `n = Integer(${v.n})\nnearp = Integer(${v.nearp})\nR.<x> = PolynomialRing(Zmod(n))\nf = nearp + x\nroots = f.small_roots(X=2**500, beta=0.5)\nif roots:\n    p = Integer(nearp + roots[0])\n    if n % p == 0:\n        q = n // p\n        print(f"SIMPLE_LATTICE=SUCCESS")\n        print(f"p={p}")\n        print(f"q={q}")\n    else:\n        print("SIMPLE_LATTICE=FAILED")\nelse:\n    print("SIMPLE_LATTICE=FAILED")`,
    proof: '\\textbf{Theorem:} If $p = p_0 + x$ where $|x| < n^{1/4}$, Coppersmith recovers $p$.\\newline\\newline\\textbf{Prerequisites:} Coppersmith method, polynomial root finding\\newline\\newline\\textbf{Proof:}\\begin{align*}p &= p_0 + x \\\\ f(x) &= p_0 + x \\pmod{n} \\\\ \\text{Coppersmith finds } &x_0 \\text{ when } |x_0| < n^{1/4}\\end{align*}\\newline\\textbf{References:} Coppersmith (1996)',
    priority: 'high',
    applicableCheck: (p) => !!p.n && !!p.nearp,
  },
  {
    id: 'partial-d',
    name: 'Partial Key Exposure',
    category: 'Partial Key / Lattice',
    description: 'Recovers factorization when low bits of d are known.',
    inputs: [
      { name: 'n', label: 'n (modulus)', placeholder: 'Enter modulus n...', multiline: true, rows: 3 },
      { name: 'e', label: 'e (public exponent)', placeholder: 'Enter public exponent e...', multiline: true, rows: 3 },
      { name: 'dLow', label: 'dLow (low bits of d)', placeholder: 'Enter known low bits of d...', multiline: true, rows: 3 },
    ],
    sageTemplate: (v) => `n = Integer(${v.n})\ne = Integer(${v.e})\ndLow = Integer(${v.dLow})\nm = dLow.bit_length()\nfound = False\nfor k in range(1, e + 1):\n    d_approx = (k * n + 1) // e\n    if d_approx % (2**m) == dLow:\n        d = d_approx\n        phi = (e * d - 1) // k\n        s = n - phi + 1\n        disc = s*s - 4*n\n        if disc >= 0:\n            sqrt_disc = isqrt(disc)\n            if sqrt_disc * sqrt_disc == disc:\n                p = (s + sqrt_disc) // 2\n                q = (s - sqrt_disc) // 2\n                if p * q == n:\n                    print(f"PARTIAL_D=SUCCESS")\n                    print(f"d={d}")\n                    print(f"p={p}")\n                    print(f"q={q}")\n                    found = True\n                    break\nif not found:\n    print("PARTIAL_D=FAILED")`,
    proof: '\\textbf{Theorem:} If low bits of $d$ are known, full $d$ can be recovered when $d$ is small.\\newline\\newline\\textbf{Prerequisites:} Hensel lifting, RSA key equation\\newline\\newline\\textbf{Proof:}\\begin{align*}ed &\\equiv 1 \\pmod{\\varphi(n)} \\\\ d &= d_{\\text{high}} \\cdot 2^m + d_{\\text{low}} \\\\ \\text{For each } k: \\quad d &\\approx \\frac{k \\cdot n}{e} \\\\ \\text{Check if } d \\bmod 2^m &= d_{\\text{low}}\\end{align*}\\newline\\textbf{References:} Boneh, Durfee, Frankel (1998)',
    priority: 'high',
    applicableCheck: (p) => !!p.n && !!p.e && !!p.dLow,
  },
  {
    id: 'partial-pq-bits',
    name: 'Partial p/q Bits',
    category: 'Partial Key / Lattice',
    description: 'Coppersmith recovers factorization when half the bits of p are known (MSB or LSB).',
    inputs: [
      { name: 'n', label: 'n (modulus)', placeholder: 'Enter modulus n...', multiline: true, rows: 3 },
      { name: 'knownBits', label: 'knownBits (known bits of p)', placeholder: 'Enter known bits as integer...', multiline: true, rows: 3 },
      { name: 'bitPosition', label: 'bitPosition', placeholder: 'msb or lsb', multiline: false },
    ],
    sageTemplate: (v) => `n = Integer(${v.n})\nknownBits = Integer(${v.knownBits})\nbitPosition = "${v.bitPosition}"\nif bitPosition == "msb":\n    k = n.nbits() // 2 - knownBits.bit_length()\n    if k < 0:\n        print("PARTIAL_PQ=FAILED: knownBits has more bits than half of n")\n    else:\n        R.<x> = PolynomialRing(Zmod(n))\n        f = (knownBits << k) + x\n        roots = f.small_roots(X=2**k, beta=0.3)\n        if roots:\n            p = Integer((knownBits << k) + roots[0])\n            if n % p == 0:\n                print(f"PARTIAL_PQ=SUCCESS")\n                print(f"p={p}")\n                print(f"q={n // p}")\n            else:\n                print("PARTIAL_PQ=FAILED")\n        else:\n            print("PARTIAL_PQ=FAILED")\nelif bitPosition == "lsb":\n    m = knownBits.bit_length()\n    R.<x> = PolynomialRing(Zmod(n))\n    f = x * (2**m) + knownBits\n    roots = f.small_roots(X=2**(n.nbits()//2 - m), beta=0.3)\n    if roots:\n        p = Integer(roots[0] * (2**m) + knownBits)\n        if n % p == 0:\n            print(f"PARTIAL_PQ=SUCCESS")\n            print(f"p={p}")\n            print(f"q={n // p}")\n        else:\n            print("PARTIAL_PQ=FAILED")\n    else:\n        print("PARTIAL_PQ=FAILED")`,
    proof: '\\textbf{Theorem:} If half the bits of $p$ are known, Coppersmith recovers the full factorization.\\newline\\newline\\textbf{Prerequisites:} Coppersmith method, polynomial rings over $\\mathbb{Z}/n\\mathbb{Z}$\\newline\\newline\\textbf{Proof:}\\begin{align*}\\text{MSB: } p &= p_{\\text{known}} \\cdot 2^k + x \\\\ \\text{LSB: } p &= x \\cdot 2^m + p_{\\text{known}} \\\\ f(x) &\\equiv 0 \\pmod{p} \\implies f(x) \\mid n \\\\ \\text{Coppersmith finds } x &\\text{ when } |x| < n^{1/4}\\end{align*}\\newline\\textbf{References:} Coppersmith (1996), Howgrave-Graham (1997)',
    priority: 'high',
    applicableCheck: (p) => !!p.n && !!p.knownBits && !!p.bitPosition,
  },
  {
    id: 'small-crt-exp',
    name: 'Small CRT Exponent',
    category: 'Partial Key / Lattice',
    description: 'When d_p or d_q is small, search and verify to factor n.',
    inputs: [
      { name: 'n', label: 'n (modulus)', placeholder: 'Enter modulus n...', multiline: true, rows: 3 },
      { name: 'e', label: 'e (public exponent)', placeholder: 'Enter public exponent e...', multiline: true, rows: 3 },
      { name: 'bound', label: 'bound (max d_p, optional)', placeholder: 'Default 1000000', multiline: false },
    ],
    sageTemplate: (v) => `n = Integer(${v.n})\ne = Integer(${v.e})\nbound = ${v.bound ? `Integer(${v.bound})` : '1000000'}\nfound = False\nfor dp in range(1, bound + 1):\n    for k in range(1, e):\n        num = dp * e - 1\n        if num % k != 0:\n            continue\n        p_candidate = num // k + 1\n        if n % p_candidate == 0:\n            print(f"SMALL_CRT_EXP=SUCCESS")\n            print(f"dp={dp}")\n            print(f"p={p_candidate}")\n            print(f"q={n // p_candidate}")\n            found = True\n            break\n    if found:\n        break\nif not found:\n    print(f"SMALL_CRT_EXP=FAILED")`,
    proof: '\\textbf{Theorem:} If $d_p = d \\bmod (p-1)$ is small, $p$ can be recovered by searching.\\newline\\newline\\textbf{Prerequisites:} RSA-CRT, modular arithmetic\\newline\\newline\\textbf{Proof:}\\begin{align*}d_p \\cdot e &\\equiv 1 \\pmod{p-1} \\\\ d_p \\cdot e - 1 &= k(p-1) \\\\ p &= \\frac{d_p \\cdot e - 1}{k} + 1 \\\\ \\text{For small } d_p, &\\text{ iterate and check } p \\mid n\\end{align*}\\newline\\textbf{References:} Standard RSA-CRT analysis',
    priority: 'medium',
    applicableCheck: (p) => !!p.n && !!p.e,
  },
  {
    id: 'dp-dq-leak',
    name: 'dp/dq Leak',
    category: 'Partial Key / Lattice',
    description: 'Given dp = d mod (p-1), iterate k to find p.',
    inputs: [
      { name: 'n', label: 'n (modulus)', placeholder: 'Enter modulus n...', multiline: true, rows: 3 },
      { name: 'e', label: 'e (public exponent)', placeholder: 'Enter public exponent e...', multiline: true, rows: 3 },
      { name: 'dp', label: 'dp (d mod p-1)', placeholder: 'Enter dp value...', multiline: true, rows: 3 },
    ],
    sageTemplate: (v) => `n = Integer(${v.n})\ne = Integer(${v.e})\ndp = Integer(${v.dp})\nfound = False\nfor k in range(1, e):\n    num = dp * e - 1\n    if num % k == 0:\n        p_candidate = num // k + 1\n        if n % p_candidate == 0:\n            print(f"DP_DQ_LEAK=SUCCESS")\n            print(f"dp={dp}")\n            print(f"p={p_candidate}")\n            print(f"q={n // p_candidate}")\n            found = True\n            break\nif not found:\n    print("DP_DQ_LEAK=FAILED")`,
    proof: '\\textbf{Theorem:} Given $d_p = d \\bmod (p-1)$, factor $n$ by iterating $k$.\\newline\\newline\\textbf{Prerequisites:} RSA-CRT parameters\\newline\\newline\\textbf{Proof:}\\begin{align*}d_p \\cdot e &\\equiv 1 \\pmod{p-1} \\\\ d_p \\cdot e - 1 &= k(p-1) \\\\ p &= \\frac{d_p \\cdot e - 1}{k} + 1 \\\\ \\text{Since } d_p < p-1, &\\text{ we have } k < e \\\\ \\text{Iterate } k = 1, \\ldots, e-1 &\\text{ and check } p \\mid n\\end{align*}\\newline\\textbf{References:} Standard RSA-CRT analysis',
    priority: 'high',
    applicableCheck: (p) => !!p.n && !!p.e && !!p.dp,
  },
  {
    id: 'linearly-related-primes',
    name: 'Linearly Related Primes',
    category: 'Partial Key / Lattice',
    description: 'When q = k*p + delta for known k and small delta.',
    inputs: [
      { name: 'n', label: 'n (modulus)', placeholder: 'Enter modulus n...', multiline: true, rows: 3 },
      { name: 'k', label: 'k (known multiplier)', placeholder: 'Enter k value...', multiline: true, rows: 3 },
    ],
    sageTemplate: (v) => `n = Integer(${v.n})\nk = Integer(${v.k})\nfound = False\nfor delta in range(-10000, 10001):\n    disc = delta*delta + 4*k*n\n    if disc >= 0:\n        sqrt_disc = isqrt(disc)\n        if sqrt_disc * sqrt_disc == disc:\n            num = -delta + sqrt_disc\n            if num > 0 and num % (2*k) == 0:\n                p = num // (2*k)\n                if n % p == 0:\n                    q = n // p\n                    print(f"LINEARLY_RELATED=SUCCESS")\n                    print(f"p={p}")\n                    print(f"q={q}")\n                    print(f"delta={delta}")\n                    found = True\n                    break\nif not found:\n    print("LINEARLY_RELATED=FAILED")`,
    proof: '\\textbf{Theorem:} If $q = kp + \\delta$ for known $k$ and small $\\delta$, solve the quadratic to factor $n$.\\newline\\newline\\textbf{Prerequisites:} Quadratic equations, Coppersmith method\\newline\\newline\\textbf{Proof:}\\begin{align*}n &= p \\cdot q = p(kp + \\delta) = kp^2 + \\delta p \\\\ kp^2 + \\delta p - n &= 0 \\\\ p &= \\frac{-\\delta \\pm \\sqrt{\\delta^2 + 4kn}}{2k} \\\\ \\text{For small } \\delta, &\\text{ iterate and check } p \\mid n\\end{align*}\\newline\\textbf{References:} Nitaj (1999)',
    priority: 'medium',
    applicableCheck: (p) => !!p.n && !!p.k,
  },
  {
    id: 'dependent-prime',
    name: 'Dependent-Prime RSA',
    category: 'Partial Key / Lattice',
    description: 'When q = e^-1 mod p. Creates solvable quadratic system.',
    inputs: [
      { name: 'n', label: 'n (modulus)', placeholder: 'Enter modulus n...', multiline: true, rows: 3 },
      { name: 'e', label: 'e (public exponent)', placeholder: 'Enter public exponent e...', multiline: true, rows: 3 },
    ],
    sageTemplate: (v) => `n = Integer(${v.n})\ne = Integer(${v.e})\nne = n * e\nfound = False\nfor k in range(1, 100000):\n    disc = 1 + 4*k*ne\n    if disc >= 0:\n        sqrt_disc = isqrt(disc)\n        if sqrt_disc * sqrt_disc == disc:\n            num = -1 + sqrt_disc\n            if num > 0 and num % (2*k) == 0:\n                p = num // (2*k)\n                if n % p == 0:\n                    q = n // p\n                    print(f"DEPENDENT_PRIME=SUCCESS")\n                    print(f"p={p}")\n                    print(f"q={q}")\n                    found = True\n                    break\nif not found:\n    print("DEPENDENT_PRIME=FAILED")`,
    proof: '\\textbf{Theorem:} If $q = e^{-1} \\bmod p$, then $n = pq$ creates a solvable quadratic system.\\newline\\newline\\textbf{Prerequisites:} Modular inverse, quadratic equations\\newline\\newline\\textbf{Proof:}\\begin{align*}q \\cdot e &\\equiv 1 \\pmod{p} \\\\ q \\cdot e &= 1 + kp \\\\ n &= p \\cdot q = p \\cdot \\frac{1 + kp}{e} \\\\ ne &= p + kp^2 \\\\ kp^2 + p - ne &= 0 \\\\ p &= \\frac{-1 + \\sqrt{1 + 4kne}}{2k}\\end{align*}\\newline\\textbf{References:} Custom CTF construction',
    priority: 'medium',
    applicableCheck: (p) => !!p.n && !!p.e,
  },
];
